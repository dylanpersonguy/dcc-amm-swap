{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "npm run build --workspace=amm-core --workspace=amm-sdk --workspace=amm-indexer"
  },
  "deploy": {
    "startCommand": "node amm-indexer/dist/server.js",
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 5
  }
}
