{
  "name": "@dcc-amm/monitor",
  "version": "1.0.0",
  "private": true,
  "description": "Health + balance monitoring for the DCC AMM stack",
  "main": "dist/index.js",
  "scripts": {
    "build": "tsc",
    "start": "node dist/index.js",
    "dev": "ts-node src/index.ts",
    "clean": "rm -rf dist"
  },
  "devDependencies": {
    "typescript": "^5.3.0",
    "ts-node": "^10.9.0",
    "@types/node": "^20.0.0"
  }
}
